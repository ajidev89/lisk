import { TIME_FRAME_POLICY_ADDRESS } from './constants.js';
import { encodePacked } from 'viem';
export const getTimeFramePolicy = (param) => {
    return {
        policy: TIME_FRAME_POLICY_ADDRESS,
        address: TIME_FRAME_POLICY_ADDRESS,
        initData: encodePacked(['uint128', 'uint128'], [BigInt(param.validUntil), BigInt(param.validAfter)]),
    };
};
//# sourceMappingURL=installation.js.map