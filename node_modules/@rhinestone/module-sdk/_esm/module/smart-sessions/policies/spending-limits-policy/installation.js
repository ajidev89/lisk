import { SPENDING_LIMITS_POLICY_ADDRESS } from './constants.js';
import { encodeAbiParameters } from 'viem';
export const getSpendingLimitsPolicy = (params) => {
    return {
        policy: SPENDING_LIMITS_POLICY_ADDRESS,
        address: SPENDING_LIMITS_POLICY_ADDRESS,
        initData: encodeAbiParameters([{ type: 'address[]' }, { type: 'uint256[]' }], [params.map(({ token }) => token), params.map(({ limit }) => limit)]),
    };
};
//# sourceMappingURL=installation.js.map