import { encodePacked } from 'viem';
import { SCHEDULED_TRANSFERS_EXECUTOR_ADDRESS } from './constants.js';
export const getScheduledTransfersExecutor = ({ executeInterval, numberOfExecutions, startDate, executionData, hook, }) => {
    return {
        address: SCHEDULED_TRANSFERS_EXECUTOR_ADDRESS,
        module: SCHEDULED_TRANSFERS_EXECUTOR_ADDRESS,
        type: 'executor',
        initData: encodePacked(['uint48', 'uint16', 'uint48', 'bytes'], [executeInterval, numberOfExecutions, startDate, executionData]),
        deInitData: '0x',
        additionalContext: '0x',
        hook,
    };
};
//# sourceMappingURL=installation.js.map