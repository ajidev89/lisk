import { Address } from 'viem';
import { Module } from '../types';
import { Session } from './types';
type Params = {
    sessions?: Session[];
    hook?: Address;
};
export declare const getSmartSessionsValidator: ({ sessions, hook, }: Params) => Module;
export {};
//# sourceMappingURL=installation.d.ts.map